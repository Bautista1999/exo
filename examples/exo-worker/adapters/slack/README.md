# Slack adapter (placeholder)

ExoWorker Slack integration is not implemented yet. The adapter worker starts,
emits a lifecycle event, and exits so `create_adapter` can register the type.

Future: Slack Bolt / Socket Mode worker with outbound `send_adapter_message` support.
